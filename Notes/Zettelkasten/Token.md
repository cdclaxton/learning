#AI 

A word may be composed of multiple tokens

Allows the [[LLM]] to deal with words it hasn't come across before and to generate non-words [Wengrow2026]

Math is more efficient with a smaller vocabulary of tokens [Wengrow2026]

Each token is converted into an **embedding vector** (where each each element is a **weight**)
