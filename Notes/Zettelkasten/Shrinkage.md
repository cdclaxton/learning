#Bayesian 

- Happens in [[Bayesian inference]] and in Maximum Likelihood Estimation
- Consequence of a hierarchical model
- In hierarchical models, estimates of low-level parameters are pulled closer together than they would be if there weren't a higher-level distribution
- Reduction in variance in the estimators relative the data
- Reduced variance only for unimodal distributions