# Deep Learning Made Easy with R book

## Setup

```R
install.packages('deepnet')
install.packages('ggplot2')
install.packages('neuralnet')
install.packages('Metrics')
```

To run an R script:

```bash
Rscript <filename>
```