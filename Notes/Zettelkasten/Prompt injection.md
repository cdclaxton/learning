#AI 

User constructs a [[Prompt]] to manipulate the LLM into deviating from its intended behaviour, e.g. disguising a system prompt.

Getting the model to reveal its system prompt, which could contain:
- Sensitive company information
- Useful information to launch attacks
