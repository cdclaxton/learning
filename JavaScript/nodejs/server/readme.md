# Simple NodeJS-based server

To run:

```bash
node server.js
```

To make the server running on a Chromebook accessible:

- Settings -> Advanced -> Developers
- Linux development environment -> Port forwarding
- Click Add
- Add port 5000