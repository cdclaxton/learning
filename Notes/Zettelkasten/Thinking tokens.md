#AI 

- Tokens of text that an [[LLM]] uses to think for itself
- The tokens cover the plan it's executing
- Could show to the user to demonstrate that it is not stalling
- Could use an LLM to rewrite them so they're more presentable
- **ReAct** technique -- instruct the LLM to reflect after each tool call on whether the tool worked properly and whether the tool advanced the plan