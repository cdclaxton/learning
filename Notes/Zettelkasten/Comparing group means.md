#Bayesian #Statistics

- Two Bayesian approaches:
	- Set of parameters for each group -- then look at the differences
	- Model selection approach: one model where each group has its own parameters, another model with shared parameters
- Might get inconsistencies in the results because with more than two groups, there are different combinations of shared parameters