// Number of permutations of N elements with M states.
int numberOfPermutations(int N, int M);

// Index in a matrix with nColumns columns.
int matrixIndex(int rowIndex,
                int colIndex,
                int nColumns);

// Permutations of N elements, each with M states.
void permutations(int N, int M, int *matrix);

void printMatrix(int *matrix,
                 int nRows,
                 int nCols);